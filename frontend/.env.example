# Optional: only needed if you deploy the frontend separately from the backend.
# During local dev the Vite proxy handles this, so these can be left unset.

# Backend REST base URL (e.g. https://api.example.com)
VITE_API_URL=

# Backend Socket.io URL (e.g. https://api.example.com)
VITE_SOCKET_URL=
